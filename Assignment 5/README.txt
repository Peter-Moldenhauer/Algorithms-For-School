To compile/run hw5.py, enter the following into the command line:
Note: This example uses input.txt as the command line argument, if your 
input file is named differently, change it accordingly in the command line...

python hw5.py input.txt 